{
  "name": "yammo-website",
  "version": "0.0.1",
  "devDependencies": {
    "gulp": "^3.9.0"
  },
  "scripts": {
    "add-submodule": "git submodule update --init && git submodule foreach git pull origin master",
    "prebuild": "git submodule foreach git pull origin master",
    "build": "gulp"
  },
  "dependencies": {
    "gulp-shell": "^0.4.3",
    "yuidoc-lucid-theme": "^0.1.1",
    "yuidocjs": "^0.9.0"
  }
}
